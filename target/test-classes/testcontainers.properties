# Enable container reuse for faster test execution
testcontainers.reuse.enable=true

# Container lifecycle management
testcontainers.ryuk.disabled=false

# Logging configuration
testcontainers.console.enable=true

# Ensure containers are not automatically stopped
testcontainers.checks.disable=true

# Container cleanup settings
testcontainers.containers.cleanup.enable=false
